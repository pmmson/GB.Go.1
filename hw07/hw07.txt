1. сталкивались c пустым интерфейсом в ф-иях fmt.Println и fmt.Scanln
   
2. в калькуляторе вместо os.Exit(1) лучше вызвать panic и обработать ошибку
    case "/":
		if b == 0 {
			panic("Второе число не должно быть равно 0! На 0 делить нельзя!")
		}
а в ф-ии main
    defer func() {
		err:= recover()
		if err != nil {
			fmt.Println(err)
		}
	}()

4. попробовал https://github.com/pmmson/GB.Go.1/pull/6
   частично воспользовался подсказкой реализации методов Write - получилось. А вот Read - нашел, попробовал реализовать аналогично, но данные остаются внутри метода и не возвращаются
   только то что получилось в pull request - возвращает данные и я могу прочитать строку через вызов метода (не через интерфейс)
   с помощью io.ReadAll() - не нашел такой функции в пакете io